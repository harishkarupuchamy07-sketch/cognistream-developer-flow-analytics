services:
  airflow:
    build: .
    container_name: cognistream-airflow
    ports:
      - "8080:8080"
    volumes:
      - ./dags:/opt/airflow/dags
      - ./:/opt/airflow/project
    command: standalone
